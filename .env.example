# Flask configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# User passwords (use strong passwords in production)
OSCAR_PASSWORD=change_me_in_production
EVA_PASSWORD=change_me_in_production

# TOTP secrets (generate using pyotp.random_base32())
OSCAR_TOTP_SECRET=your_totp_secret_here
EVA_TOTP_SECRET=your_totp_secret_here

# Optional: Database URL (if you plan to use a database later)
# DATABASE_URL=sqlite:///neirapinuela.db

# Optional: Email configuration (for future features)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password